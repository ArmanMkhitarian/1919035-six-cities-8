export enum AppRoute {
  Login = '/login',
  Favorites ='/favorites',
  FavoritesEmpty = '/favorites-empty',
  Root = '/',
  Property = '/offer:id',
  Main = '/main'
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
